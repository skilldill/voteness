export interface User {
    id: string,
    username: string,
    email: string
}

export interface UserInfo {
    id: string,
    name?: string,
    surname?: string,
    description?: string,
    avatar?: any
}